name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build executables
        run: |
          bun build ./src/client.ts --compile --target=bun-linux-x64 --outfile=spacemolt-client-linux-x64
          bun build ./src/client.ts --compile --target=bun-linux-arm64 --outfile=spacemolt-client-linux-arm64
          bun build ./src/client.ts --compile --target=bun-darwin-x64 --outfile=spacemolt-client-macos-x64
          bun build ./src/client.ts --compile --target=bun-darwin-arm64 --outfile=spacemolt-client-macos-arm64
          bun build ./src/client.ts --compile --target=bun-windows-x64 --outfile=spacemolt-client-windows-x64.exe

      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: release-binaries
          path: |
            spacemolt-client-linux-x64
            spacemolt-client-linux-arm64
            spacemolt-client-macos-x64
            spacemolt-client-macos-arm64
            spacemolt-client-windows-x64.exe

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: SpaceMolt Client v${{ steps.version.outputs.VERSION }}
          generate_release_notes: true
          files: |
            artifacts/release-binaries/spacemolt-client-linux-x64
            artifacts/release-binaries/spacemolt-client-linux-arm64
            artifacts/release-binaries/spacemolt-client-macos-x64
            artifacts/release-binaries/spacemolt-client-macos-arm64
            artifacts/release-binaries/spacemolt-client-windows-x64.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
